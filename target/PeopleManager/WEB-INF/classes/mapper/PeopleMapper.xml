<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cdp.dao.PeopleDao">
    <select id="login" resultType="People">
        select P_account,P_password,P_truename from people where P_account = #{P_account} and P_password = #{P_password}
    </select>
</mapper>